repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.0
    hooks:
      - id: ruff          # linter — fails on errors, auto-fixes what it can
        args: [--fix]
      - id: ruff-format   # formatter (like black)
